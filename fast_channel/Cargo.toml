[package]
name = "fast_channel"
version = "0.1.0"
edition = "2021"

[lib]
name = "_fast_channel"
crate-type = ["cdylib"]

[dependencies]
pyo3 = { version = "0.24", features = ["extension-module"] }
parking_lot = "0.12"
